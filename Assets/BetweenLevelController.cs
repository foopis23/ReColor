using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class BetweenLevelController : MonoBehaviour
{
    private TMP_Text level_title;
    private TMP_Text par_score;
    private TMP_Text max_rewind;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
